import { forwardRef } from "react";
const visuals = { cat:{ face:"🐱", tail:"〰", label:{ th:"แมว PAP", en:"PAP cat" } }, dog:{ face:"🐶", tail:"⌁", label:{ th:"หมา PAP", en:"PAP dog" } } };
const Mascot = forwardRef(function Mascot({ kind, language, onActivate }, ref) { const visual = visuals[kind]; return <button ref={ref} className={`pap-mascot pap-mascot--${kind}`} data-kind={kind} data-state="idle" type="button" aria-label={language === "en" ? `${visual.label.en} — open interaction menu` : `${visual.label.th} — เปิดเมนู interaction`} onClick={() => onActivate(kind)}><span className="pap-mascot__shadow" aria-hidden="true" /><span className="pap-mascot__tail" aria-hidden="true">{visual.tail}</span><span className="pap-mascot__face" aria-hidden="true">{visual.face}</span></button>; });
export default Mascot;
